/**
 * Matches `subscriptions.status` when a user is treated as “Active” on the
 * Users page (Stripe). Keep in sync with billing writes.
 */
export const USERS_SUBSCRIPTION_STATUS_ACTIVE = "active" as const;
