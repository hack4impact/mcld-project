{
	"name": "mcld-services",
	"version": "0.2.0",
	"description": "MCLD Services widget for Elementor.",
	"author": "MCLD",
	"license": "GPL-2.0-or-later",
	"private": true,
	"engines": { "node": ">=22" },
	"scripts": {
		"build": "node scripts/build.mjs",
		"plugin-zip": "npm run build && node scripts/package.mjs",
		"test": "node --test tests/*.test.mjs"
	},
	"devDependencies": {
		"esbuild": "^0.28.2",
		"fflate": "^0.8.3",
		"jsdom": "^26.1.0",
		"sass": "^1.105.0"
	}
}
